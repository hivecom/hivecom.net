export interface ActivityComponentProps {
  /**
   * If set to true, activity will just appear as an icon with more information
   * in the tooltip. Used for user rich presence hover card.
   */
  small?: boolean
}
