# direnv config: automatically load the repository's Nix dev shell
# Requires: direnv + nix-direnv
#
# First time in this repo, run:
#   direnv allow

# Load the Nix shell defined in nix/shell.nix
use nix .nix/shell.nix
